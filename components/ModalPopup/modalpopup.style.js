import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

    backdrop: {
        position: 'absolute',
        top: 0,
        left: 0,
        bottom: 0,
        right: 0,
        backgroundColor: '#000'
    },
});
export default styles;